// Forward context definitions and AuthProvider directly to the new AuthContext in src/
export { AuthContext, AuthProvider } from "../../src/context/AuthContext";

export default function AuthRoutePlaceholder() {
  return null;
}
